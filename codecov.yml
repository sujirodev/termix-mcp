# Informational only: comments on the PR, never fails it (plano-implementacao.md Fase 5).
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

ignore:
  - "tests/**"

comment:
  layout: "condensed_header, diff, files"
  require_changes: true
